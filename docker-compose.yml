services:
  nats:
    image: nats:2.10-alpine
    container_name: watchdog-nats
    ports:
      - "4222:4222"   # client connections
      - "8222:8222"   # HTTP monitoring
    command: ["--http_port", "8222"]
    restart: unless-stopped
